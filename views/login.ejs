<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Stumni</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --stumni-dark-blue: #0A2540; --stumni-gold: #FFC107; }
        body { font-family: 'Inter', sans-serif; background-color: var(--stumni-dark-blue); }
        .login-card { border-radius: 1rem; border: none; }
        .form-control:focus, .form-select:focus { border-color: var(--stumni-gold); box-shadow: 0 0 0 0.25rem rgba(255, 193, 7, 0.25); }
        .btn-stumni { background-color: var(--stumni-gold); border-color: var(--stumni-gold); color: var(--stumni-dark-blue); font-weight: 600; }
        .btn-stumni:hover { opacity: 0.9; color: var(--stumni-dark-blue); }
    </style>
</head>
<body>
    <div class="container">
        <div class="row vh-100 justify-content-center align-items-center">
            <div class="col-12 col-md-8 col-lg-6 col-xl-5">

                <div class="text-center mb-4">
                    <div class="d-flex align-items-center justify-content-center">
                        <img src="/Assets/logo.png" alt="Stumni Logo" class="me-0" style="height: 5rem; width: 5rem;">
                        <span class="fs-1 fw-bold text-white">Stumni</span>
                    </div>
                </div>

                <div class="card login-card p-4">
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <h1 class="h3 fw-bold text-dark">Welcome!</h1>
                            <p class="text-muted small">Log in to connect with your community.</p>
                        </div>
                        <form action="/login" method="POST">
                            <% if (typeof error !== 'undefined' && error) { %>
                                <div class="alert alert-danger py-2" role="alert"><%= error %></div>
                            <% } %>
                            <div class="form-floating mb-3">
                                <input type="email" name="email" class="form-control" id="email" placeholder="name@example.com" required>
                                <label for="email">Email</label>
                            </div>
                            <div class="form-floating mb-3">
                                <input type="password" name="password" class="form-control" id="password" placeholder="Password" required>
                                <label for="password">Password</label>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-stumni">Log In</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>